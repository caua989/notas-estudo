## veerificação de chave SSH

git --version

git config --global user.name "Seu nome"
git config --global user.email "Seu email@gmail.com

Is -al ~/.ssh

shh-keygen -t ed25519 -C "your_email@exaple.com

eval "$(ssh-agent -s)

## Como passar um repositorio para o VSC

git bash
 cd Documents/(embaixo das aspas/enter)
 git clone (botao direito do mouse) ((paste)url copiada do code SHH)
 cd (nome do repositorio)/
 code .

## Configurar "index.html"

# 1- Criação base
!(Enter)
 Deixe a linguagem em "pt-br"
 Va ate os "Body"

 # 2- Titulo
 Coloque "h2"
 Coloque o titulo dentro das "><"

# 3- Configuração de botoes
 Coloque "Button"
 Dentro o nome que as pessoas verao do botão dentro das "><"
 Coloque dois espaços entre o 1 "Button" e o ">"
 Entre os dois espaços coloque "id"
 Entre as aspas do "id = ("")" Cooloque o nome do botao(Tudo junto e sem asento)

 # 4- Seleção de file
 Escolha a opção "script:src"
 Dentro das aspas coloque "./(fowder/file)"

 ## Resolução de exercicio

# 1- Function
Escreva "function NomeDaQuestao () {}"
Separe as linhas do "{}"

# 2- Questão em si
Na linha vazia entre os "{}" resolva a questão

# 3- getElementById
Na linha abaixo do fim do "{}" escreva "const buttonExemplo1 = document.getElementById("nome do botão")

# 4- addEventListener
Uma linha abaixo do "getElementById" escreva "buttonExemplo1.addEventListener('click', () => { NomeDaQuestao() })

## =======================================================================================================================================
## Atalhos

Ctrl+(k+o) (abrir projetos)

crtl+barra de spaço 

## Uso de "if"

if: se
else if: se não, então se
else: se ainda nao, então

## Garantias

console.log